package model.exceptions.model_docs_exceptions;

public class TicketDocNotFoundException extends DocNotFoundException {
    public TicketDocNotFoundException(String msg) {
        super(msg);
    }
}
